<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/title_bar"
    android:layout_width="match_parent"
    android:layout_height="45dip"
    android:background="#fff"
    android:orientation="horizontal" >

    <TextView
        android:id="@+id/titlebar_title"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:gravity="center_vertical"
        android:text="标题"
        android:textColor="#000"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/titlebar_commint"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="10dip"
        android:gravity="center"
        android:text="发送"
        android:textColor="#000"
        android:textSize="16sp"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/titlebar_back_ll"
        android:layout_width="50dip"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:gravity="center"
        android:orientation="vertical" >

        <ImageButton
            android:id="@+id/titlebar_back"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#00000000"
            android:src="@mipmap/icon_back" />
    </LinearLayout>

    <ImageButton
        android:id="@+id/titlebar_problem"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="10dip"
        android:background="@null"
        android:visibility="gone" />

    <ImageButton
        android:id="@+id/titlebar_problem2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/titlebar_problem"
        android:layout_marginRight="15dp"
        android:layout_toLeftOf="@+id/titlebar_problem"
        android:background="@null"
        android:visibility="gone" />

</RelativeLayout>